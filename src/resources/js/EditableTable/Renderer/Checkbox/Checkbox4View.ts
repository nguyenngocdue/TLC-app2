import { TableRendererParams } from '../../Type/EditableTable3DataLineType'

export class Checkbox4View {
    constructor(private params: TableRendererParams) {}

    render() {
        return { rendered: 'Checkbox4 View' }
    }
}
